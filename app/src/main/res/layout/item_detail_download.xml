<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/item_detail_download"
    android:background="@drawable/detail_bottom_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:orientation="horizontal"
        android:paddingBottom="5dp"
        android:paddingTop="5dp">


        <!-- 收藏button -->

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_margin="6dp"
            android:src="@mipmap/detail_share"/>



        <!-- 下载区域 -->

        <org.me.appstore.views.ProgressBtn
            android:id="@+id/app_detail_download_btn_download"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_centerVertical="true"
            android:paddingTop="5dp"
            android:paddingBottom="5dp"
            android:text="下载"
            android:background="@drawable/detail_btn"
            android:textColor="@android:color/white" />

        <!-- 分享button -->

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_margin="6dp"
            android:src="@mipmap/detail_collection_off"/>
    </LinearLayout>

</FrameLayout>